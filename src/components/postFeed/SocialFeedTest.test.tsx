import React from 'react';
import PostList from './postList';
import renderer from 'react-test-renderer';

const testData = [
    {postId: 'JQWERY', user: 'Test User', body: 'I wanted to do a test so I entered test data into the test data constant in the test post file so that the text I typed would appear in the tested text test.'}
]

describe('<PostList />', () => {

})